
            # 7. Inputting amount of lives between 1 to 10: 

                while errorcounter < 1 or errorcounter > 10:
                    try:
                        print()
                        print("Please enter the amount of lives you will give yourself to")
                        errorcounter = int(input("guess my number (between 1 to 10): "))

                        # Error correction loop:

                        if errorcounter < 1 or errorcounter > 10:
                            invalid_input()

                    # If the user does not input an integer:

                    except ValueError:
                        error_correction()

            # Counting amount of total rounds + Identifying the user's selected mode:

            counteragain = counteragain + 1
            if mode != '0':
                print()
                print("Okay. INFINITE MODE has been activated. Now let's play", name+"!")
            else:
                print()
                print("Alright", name+". Now let's play!")
